# Images and PDFs (source files, not generated assets)
*.png
*.pdf

# Claude / agent config (local only)
.claude/
.agents/

# Persistent Claude memory — never committed
memory/

# Claude Code agent state
skills-lock.json

# Throwaway dev artefacts
ltf_report.html
tempe_map_fragment.svg
serve.py

# D1 backups + match.js review output — large, regenerable, not source.
# (The human relations were rescued from backups/ into db/human-relations.json.)
backups/
review/

# macOS metadata
.DS_Store

# Node
node_modules/

# Local env — contains API keys, never commit
.env

# Wrangler local state
.wrangler/
