# meetings/ — hand-built level-3 detail pages

Deep-dive detail pages for complex items (multi-street works, timelines, resident action). Most items
never get one — they get a home-page card + an infocouncil link. Only build a detail page when an item
genuinely needs it, and **never publish a half-finished one** (see `memory/design.md`).

| Path | What it is |
|---|---|
| `ltf-18may2026/tempe-south/index.html` | The Tempe South LATM page — the **reference** for level-3 detail. ⚠️ **Do not touch it.** Street-level detail is mirrored in `memory/ltf-tempe-detail.md`. |

Open issues on this page: #11 (bug: map issues) and #10 (bug: navigation trail) — low priority.

**Structural change → update this MAP + the `meetings/` hook in the repo-root `MAP.md`.**
